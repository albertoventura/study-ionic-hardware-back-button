import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'hardware-back-button',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
